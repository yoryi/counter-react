import React from 'react';
//creates an initialized state = 0 (hooks = useState)

//create two buttons to increase and decrease the state

function Counter() {
  return (
    <div>
    </div>
  );
}
export default Counter;
